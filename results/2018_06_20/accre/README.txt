rf_oversampled is a test of running tasks on accre cluster using random forest oversampling

svc_grid search contains results from running a grid search to tune hyperparameters for the model.
output.txt contains the actual parameters used within that folder.

svc_grid_search_test is a test version to test for resource usage on accre.