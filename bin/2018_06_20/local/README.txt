Linear SVC is linear support vector machine run on the full data set.

grid_search uses a grid search with cross validation f1 scores to find the best hyperparameters for the SVC model run on a test data set that is much smaller than the full data set.