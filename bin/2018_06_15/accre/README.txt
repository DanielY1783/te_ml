Test version is to run on ACCRE to get how many resources to request.
test.slurm sends the test version to the slurm scheduler.

Full version runs on all the data.
full.slurm sends the full version to the slurm scheduler.

Files are in both jupyter notebook format (for ease of editing) and .py format (for ease of running on ACCRE cluster).